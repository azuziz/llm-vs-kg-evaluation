# Edge output diagnostics

Per-question edge-count summaries (163 questions).

## GPT output volume

- **v1**: mean(mean_edges)=0.676, median(mean_edges)=1.010, mean(zero_edge_rate)=0.424
- **v2**: mean(mean_edges)=0.748, median(mean_edges)=1.000, mean(zero_edge_rate)=0.252
- **v3**: mean(mean_edges)=0.218, median(mean_edges)=0.040, mean(zero_edge_rate)=0.784
- **v4**: mean(mean_edges)=0.423, median(mean_edges)=0.280, mean(zero_edge_rate)=0.577

## ROBOKOP density

- **ROBOKOP**: mean edges per question = 2.196, median = 2.000, min=0, max=9
